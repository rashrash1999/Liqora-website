# Server secrets belong in Firebase Secret Manager, never in client JavaScript.
# Create TICKET_SIGNING_KEY using Firebase CLI before deploying ticket functions.
# No database password is needed for Firestore.
# Public Firebase web configuration belongs in js/firebase-config.js.
# Payment and WhatsApp providers have not been selected or implemented.
