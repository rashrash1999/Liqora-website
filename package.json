{
  "name": "liqora-invitations",
  "version": "1.1.0",
  "private": true,
  "description": "Liqora invitation storefront with secure paid-order notifications through Unifonic.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node scripts/build.js",
    "check": "node scripts/check.js",
    "test": "node --test tests/*.test.js"
  }
}
